<div class="row">
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Top Movies</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @top_movies.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @top_movies.each do |item| %>
              <tr>
                <td><img height='30%' width='50%' src='http://178.63.89.75:8181/pms_image_proxy?img=<%= item["thumb"] %>&fallback=poster&X-Plex-Token=PJ8iyEqEm2o3pZSkh5fy'></td>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Top TV</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @top_tv.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @top_tv.each do |item| %>
              <tr>
                <td><img height='30%' width='50%' src='http://178.63.89.75:8181/pms_image_proxy?img=<%= item["grandparent_thumb"] %>&fallback=poster&X-Plex-Token=PJ8iyEqEm2o3pZSkh5fy'></td>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Top Music</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @top_music.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @top_music.each do |item| %>
              <tr>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Popular Movies</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @popular_movies.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @popular_movies.each do |item| %>
              <tr>
                <td><img height='30%' width='50%' src='http://178.63.89.75:8181/pms_image_proxy?img=<%= item["thumb"] %>&fallback=poster&X-Plex-Token=PJ8iyEqEm2o3pZSkh5fy'></td>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Popular TVs</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @popular_tv.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @popular_tv.each do |item| %>
              <tr>
                <td><img height='30%' width='50%' src='http://178.63.89.75:8181/pms_image_proxy?img=<%= item["grandparent_thumb"] %>&fallback=poster&X-Plex-Token=PJ8iyEqEm2o3pZSkh5fy'></td>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Popular Music</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <% if @popular_music.empty? %>
              <tr><th>No data</th></tr>
          <% else %>
              <tr><th>Title</th><th>Total plays</th></tr>
          <% end %>
          <% @popular_music.each do |item| %>
              <tr>
                <td><%= item["title"] %></td>
                <td><%= item["total_plays"] %></td>
              </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
